# Este programa permite que el
# usuario ingrese el valor anterior y
# actual de un medidor de luz para
# asi calcular el monto

# valorAnterior, valorActual, monto son reales

valorAnterior = float(input())
valorActual = float(input())
monto = (valorActual - valorAnterior) * 125.6